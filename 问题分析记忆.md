# 《CHAOS;CHILD》Winlator 启动器校验问题 —— 完整分析记忆

> 本文件为技术备忘，供在任意环境继续分析/复现。所有偏移、字节、结论均来自 PC 实测与逆向验证。
> 最后更新：2026-09-03

---

## 0. 一句话结论

`Game.exe` 内部有「启动器校验」，依赖**共享内存(文件映射)握手**；在 Winlator 的 Wine 环境下该握手失败，导致弹「ランチャーから起動してください。」。
**已用 1 字节补丁（jne→jmp，file offset `0x6B418`）跳过校验**，补丁后直接运行 `Game.exe` 即可进入游戏，跨环境（Windows/Wine/Winlator）有效。

---

## 1. 环境与文件

| 项 | 值 |
| --- | --- |
| 游戏目录（注意含分号） | `D:\Download\CHAOS;CHILD` |
| 修复方案文件夹 | `D:\Download\CHAOS;CHILD\CHAOS;CHILD_winlator_fix` |
| 分析主机 | Windows，PowerShell 5.x，Python 3.14.7（`D:\Python\Python 3.14\python.exe`），capstone-5.0.9 |
| GitHub 公开仓库 | `https://github.com/isTurn/chaoschild-winlator-fix` |

### 关键文件
| 文件 | 说明 |
| --- | --- |
| `Game.exe` | 2016-03-18 构建，x86 PE32，**当前已打补丁**（9969152 字节） |
| `Game.exe.orig` | **补丁前原版备份**（还原用） |
| `Game.exe.bak` | 2011-04-18 旧版残留，**与当前版不同，不可当还原源** |
| `启动游戏.exe` | 2016 汉化启动器（MFC，窗口类 `MAGESgamelauncher`），内含启动串 `Game.exe chaoschild` |
| `Launcher.exe.bak` | 原版日文启动器（改回无效，因为握手在 Wine 下一样失败） |
| `USRDIR\*.mpk` | 汉化文本 |
| config.dat | `C:\Users\Turn\Documents\My Games\mages_dmm\CHAOS CHILD\config.dat`（108 字节二进制） |

---

## 2. 报错信息与字符串定位（Game.exe）

| 内容 | file offset | RVA | 编码 |
| --- | --- | --- | --- |
| 标题「エラー」 | `0xB84C0` | `0xB96C0` | cp932 |
| 正文「ランチャーから起動してください。」 | `0xB84C8` | `0xB96C8` | cp932 |
| `MultiplexChaoschild`（防多开互斥体名） | `0xB84EC` | `0xB96EC` | ASCII |
| 「既に起動しています。」（防多开报错） | — | `0xB9700` | cp932 |

---

## 3. 根因分析（已证实）

### 3.1 决定性实验（PC 实测）
| 启动方式 | 结果 |
| --- | --- |
| 直接运行 `Game.exe`（无参数） | 弹「エラー」 |
| 直接运行 `Game.exe chaoschild`（带参数） | 仍弹「エラー」 |
| 经「启动游戏.exe」点「开始游戏」 | **正常进游戏**，窗口标题 `DIEmWin | CHAOS;CHILD` |

- 启动器实际执行命令 = `Game.exe chaoschild`（WMI 捕获）。
- 同一命令，经启动器成功、直接执行失败 → 命令行参数不是充分条件。
- `MultiplexChaoschild` 是 **Game.exe 自己创建的防多开互斥体**（游戏运行时才存在），**不是**握手对象。

### 3.2 校验函数（.text，已逆向）
函数范围：VA `0x46BF80` ~ `0x46C069`（RVA `0x6BF80`）。逻辑：

```
0046BF80  ...打开共享内存映射对象（名字参数来自 eax，类型文件映射）
0046BFA1  byte[ebp-1] = 0            ; bl 初值 = 失败
0046BFB0 循环（最多 0xB4=180 次，含等待/重试）：
0046BFB3    eax = [edi+4]             ; 从共享内存读标记值
0046BFB8    cmp eax, [0xcf2fb4]       ; 与常量比较
0046BFBE    je 0x46BFFC               ; 相等 -> bl=1（校验通过）
0046BFF7  bl = [ebp-1]                ; 循环耗尽 -> bl=0（失败）
0046C015  test bl, bl
0046C018  jne 0x46C034                ; ★ 补丁点：bl≠0 才放行
0046C01A  push 0x10 / 0x4B96C0 / 0x4B96C8 / 0
0046C028  call [0x4B31B4]             ; MessageBoxA（弹「请从启动器启动」）
0046C02E  xor al, al; ret             ; 返回失败
0046C034  push 0x4B96EC               ; "MultiplexChaoschild"
0046C03E  call 0x48EF60               ; 防多开检查
0046C043  cmp eax, 0x80000000
0046C048  jne 0x46C064
0046C058  call [0x4B31B4]             ; 弹「既に起動しています」并返回失败
0046C064  mov al, 1; ret              ; 返回成功
```

### 3.3 补丁（1 字节）
| 位置 | 修改 |
| --- | --- |
| file offset `0x6B418`（VA 0x46C018） | `75 1A`(jne) → `EB 1A`(jmp)，无条件跳过校验错误框，直接进正常启动路径 |

- 验证：补丁后**直接运行 `Game.exe`（无启动器、无参数）→ 主窗口标题 `CHAOS;CHILD`，正常进入**。
- 备份：`Game.exe.orig`。还原=改回 `75 1A` 或覆盖备份。

---

## 4. PE 逆向要点（复现用）

- **ImageBase = `0x400000`**（关键！早期用相对 RVA 搜 `FF 15` 失败，必须用**完整地址** `ImageBase + IAT_RVA`）。
- 代码里调用为 `call dword ptr [完整地址]`，如 `call [0x4B31B4]`。
- 段表：
  - `.text` RVA `0x1000` / raw `0x400`（大小 0xB1A00）
  - `.rdata` RVA `0xB3000` / raw `0xB1E00`
  - `.data` RVA `0xE4000` / raw `0xE2600`
  - `_RDATA` RVA `0x3589000` / raw `0x8FA400`
- 关键 IAT RVA（.rdata 内）：`CreateFileMappingA@0xB301C`、`MapViewOfFile@0xB3014`、`CreateMutexA@0xB304C`、`GetCommandLineA@0xB30BC`、`GetStartupInfoW@0xB3178`、`GetEnvironmentStringsW@0xB3154`、`CreateFileW@0xB305C`、`GetModuleFileNameA@0xB3144`、**`MessageBoxA@0xB31B4`**。
- 逆向方法：`python -m pip install capstone`（5.0.9 兼容 Python 3.14）；反汇编 .text，扫描 `FF 15`/`FF 25` + 完整地址（`0x4xxxxxxx`），映射回 IAT 得 API 调用点；MessageBoxA 共 3 个调用点，校验错误框在 text idx `0x6B028` 处（RVA `0x6C028`）。

### 启动器（启动游戏.exe）关键字符串
- UTF-16：`CHAOS CHILD`(0x184938)、`%s\My Games`、`%s\mages_dmm`、`config.dat`、`_Mutex`(0x1849AC)、`CHAOS;CHILD`、`MAGES.`、`MAGESgamelauncher`。
- 启动器同样走 GetProcAddress 动态调用（静态搜 `call [IAT]` 无果）。

---

## 5. 未决问题（继续分析的方向）

1. **共享内存的确切名字**未知。校验通过 `MapViewOfFile`(IAT 0xB3014) 打开对象，名字参数来自函数入口 `eax`，未能回溯到字面量。候选方向：断点调试 `CreateFileMappingW/OpenFileMappingW`，或从启动器 `_Mutex` 拼接规律反推。
2. **握手标记值**：Game 比较 `[edi+4]` 与全局 `[0xcf2fb4]`；该常量在 `.data` 大区（RVA 0xE4000+），来源（启动器写入 or 内置）未确认。
3. Winlator/Wine 下握手失败的**具体 API 层原因**未定位（Wine 对命名文件映射的支持差异？名字含 `\`？），补丁已绕开，不影响使用。

---

## 6. Winlator 使用注意事项（用户实测）

- 补丁后的 `Game.exe` 覆盖到 Winlator 容器内游戏目录。
- **首次需先通过「启动游戏.exe」点一次「开始游戏」；之后可直接点击 `Game.exe` 启动**，无需每次经启动器。
- 不再依赖共享内存握手，因此 Winlator 下不再报「ランチャーから起動してください」。

---

## 7. 产物与辅助工具

- 一键补丁脚本：`CHAOS;CHILD_winlator_fix\patch.py`（校验字节→自动备份→写入 `EB 1A`）。
- 说明文档：`CHAOS;CHILD_winlator_fix\README.md`、报错截图 `error_screenshot.png`。
- 诊断截图（用户上传）：`D:\Download\CHAOS;CHILD\屏幕截图 2026-09-02 145159.png`。

## 8. 版权合规备忘

- 游戏本体（Game.exe、USRDIR、manual.pdf、汉化补丁等）受版权保护，**不上传 GitHub**；仓库只含修复方案 + patch.py。
- 若需分享补丁版 Game.exe，走网盘私发等渠道，勿挂公开仓库。
